name: ci
on:
  push:
    branches-ignore:
      - master
  pull_request:

jobs:
  integration:
    name: Job name
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        package: ['a', 'b']

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Step name
        run: exit 1
